apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loadgen-k6.fullname" . }}-test-connection"
  labels:
    {{- include "loadgen-k6.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "loadgen-k6.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
