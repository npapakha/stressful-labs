apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sut-spring-jdbc.fullname" . }}-test-connection"
  labels:
    {{- include "sut-spring-jdbc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sut-spring-jdbc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
