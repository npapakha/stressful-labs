apiVersion: v1
kind: Service
metadata:
  name: {{ include "loadgen-jmeter.fullname" . }}
  labels:
    {{- include "loadgen-jmeter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "loadgen-jmeter.selectorLabels" . | nindent 4 }}
