apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "loadgen-jmeter.fullname" . }}-test-connection"
  labels:
    {{- include "loadgen-jmeter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "loadgen-jmeter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
